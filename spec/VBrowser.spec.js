import Vue from 'vue'
import VBrowser from '../src/index.js'
 
Vue.config.productionTip = false; // Turn off production message so it doesn't show in command window.
 
describe('Browser', () => {
  it('returns the current browser', () => {
  });
});
